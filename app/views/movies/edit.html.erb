<h1>Edit Movie</h1>

<% if @movie.errors.any? %>
  <div style="background-color: #fee; border: 1px solid #fcc; border-radius: 4px; padding: 15px; margin-bottom: 20px; color: #c33;">
    <h3 style="margin-top: 0; color: #c33;">❌ Cannot update the movie:</h3>
    <ul style="margin: 10px 0;">
      <% @movie.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: @movie do |f| %>

  <p>
    <label>Title:</label> <%= f.text_field :title %>
  </p>

  <p>
    <label>Rating:</label>
    <%= f.select :rating, (0..5).to_a %>
  </p>

  <p>
    <label>Description:</label> <%= f.text_area :description %>
  </p>

  <p>
    <label>Release Date:</label> <%= f.date_select :release_date %>
  </p>

  <div style="margin-top: 20px;">
    <%= f.submit "Update Movie", style: "padding: 10px 20px; background-color: #28a745; color: white; border: none; border-radius: 4px; cursor: pointer; margin-right: 10px;" %>
    <%= link_to "Back to Movie", movie_path(@movie), style: "padding: 8px 16px; background-color: #007bff; color: white; text-decoration: none; border-radius: 4px; display: inline-block;" %>
  </div>

<% end %>